# Python
__pycache__/
*.pyc
.venv/
.venv-check/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.env

# Node
node_modules/
.next/
next-env.d.ts

# Misc
.DS_Store
*.log

# TS build cache
*.tsbuildinfo

# host claude bridge runtime pid
.bridge.pid
.playwright-host.pid
.playwright-mcp/

# Raw webm is just a Playwright intermediate; keep only the H.264 MP4 in git.
demo/*.webm

# Local-only — not pushed to the public repo.
# docs/ : the canonical markdown sources (architecture, plan, local-setup,
#         submission/understanding) — kept on the maintainer's machine for
#         editing; PDFs are uploaded to the submission form by the maintainer.
# submission/ : the generated PDFs.
docs/
submission/
